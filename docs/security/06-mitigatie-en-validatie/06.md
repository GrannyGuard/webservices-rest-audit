# Mitigatie & validatie verbeteringen

Rubric: *"Verbeteronderzoek security"* — **Mitigatie & validatie verbeteringen** (20 pt).

Dit document beschrijft per kwetsbaarheid de genomen **mitigatie**, hoe die is
**gerealiseerd** (incl. (AI-)tooling, §6) en hoe **gevalideerd** is dat het risico is
verlaagd (voor/na). De validatie steunt op drie reproduceerbare bronnen:

1. **CodeQL-herscan** (SAST) — een alert gaat van `open` → `fixed`;
2. **Unit tests** (JUnit) — bewijzen het gedrag van de fix;
3. **Build** (`mvn -B clean install`) — bevestigt dat de wijziging compileert en geen
   regressie in de bestaande tests veroorzaakt.

> Pentest-**hervalidatie** van de meest kritische bevindingen (de "na"-meting met
> dezelfde aanvalsstappen) hoort in [`05-penetration-tests`](../05-penetration-tests/)
> en wordt in de testomgeving uitgevoerd; zie ook de restpunten onderaan.

---

## Overzicht

| # | Bevinding | Type mitigatie | Validatie (deze branch) | Status |
|---|---|---|---|:---:|
| 1 | CWE-117 — log injection (F1) | code (centrale sanitizer) | CodeQL 9 → 0 alerts | ✅ geverifieerd |
| 2 | SQ8 / CWE-79 — XSS in Swagger debug | config (gating only) — code-fix bewust **niet** gemitigeerd | Endpoint niet testbaar in pentest (Swagger → 404); validatie niet mogelijk | ❌ Open |
| 3a | CWE-302 — auth-filter fall-through (§3a) | code (`return` na 401) | 12 filter-tests groen | ✅ code-fix |
| 3b | `java/user-controlled-bypass` #908 (§3b) | geaccepteerd risico (FP) | niet exploiteerbaar; upstream optionele Basic-Auth, faalt gesloten | ⏳ onderbouwde dismissal |
| 4 | SQ9 — Swagger host-header injectie | code (Host-allow-list + scheme-sanitisatie) | 8 unit tests groen; build | ✅ geverifieerd |
| 5 | Attack-surface-reductie (preventief) | config/CI (gating, least-priv DB, signing, SHA-pin) | zie hardening-checklist | ✅ mechanisme |
| 6 | CWE-307 — geen rate limiting (F-01) | code (in-memory IP-rate-limiter in `AuthorizationFilter`) | HTTP 429 op Kali bevestigd; 6 unit tests groen | ✅ geverifieerd |
| 7 | CWE-204 — gebruikersenumeratie (F-02) | code (privilege check op `/user`; uniforme AUTH_FAILURE log) | Privilege-blokkade + uniforme logs op Kali bevestigd; 3 unit tests groen | ✅ geverifieerd |

---

## 1. CWE-117 Log injection

**Bevinding:** [04-code-review §2.1 — CR-1](../04-code-review/04.md#21-bevinding-1--log-injection-cwe-117-via-rest-request-parameters)
(CodeQL `java/log-injection`, 9 alerts, `security-extended` suite — zie
[ADR-001](../02-secure-pipelines/adr-001-codeql-query-suite-scope.md)).

**Mitigatie:**
- Nieuwe gedeelde helper `RestUtil.sanitizeForLog(String)`
  (`omod-common/.../web/RestUtil.java`) verwijdert `\r`/`\n` uit user-controlled
  waarden vóórdat ze in een logregel terechtkomen — dit is het door CodeQL
  herkende sanitization-patroon voor `java/log-injection` (taint-barrier).
- Toegepast op alle 8 instromende call-sites in
  `RestUtil.getIntegerParam`, `ClearDbCacheController2_0` (×3),
  `SearchIndexController2_0` (×3) en `ConceptResource1_8`.
- **Realisatie/tooling:** geïmplementeerd met Claude Code (Sonnet 4.6) — de
  CodeQL-alertlijst (via `gh api .../code-scanning/alerts`) is gebruikt om de
  exacte bestanden/regels te identificeren; de fix zelf (één centrale sanitizer
  + toepassing op de call-sites) is handmatig gereviewd voordat gecommit.
- **Commit:** `831dafb` — `fix(security): sanitize user input before logging
  (CWE-117 log injection)`

**Validatie (voor/na):**

Bron: [`codeql.yml` op `main`, commit `e476489`, 2026-06-10](https://github.com/GrannyGuard/webservices-rest-audit/actions/runs/27280969725)
(`gh api repos/.../code-scanning/alerts`, gefilterd op `rule.id == "java/log-injection"`).

| | Vóór | Na |
|---|---|---|
| Open `java/log-injection`-alerts | 9 | **0** |
| Alerts auto-gesloten door GitHub (`state: fixed`, 2026-06-10T13:55:43Z) | n.v.t. | **9/9** — alert-nrs `7`–`15` |

| Alert # | Bestand:regel | Status |
|---:|---|---|
| 7 | `ClearDbCacheController2_0.java:79` | ✅ fixed |
| 8 | `ClearDbCacheController2_0.java:86` | ✅ fixed |
| 9 | `ClearDbCacheController2_0.java:95` | ✅ fixed |
| 10 | `SearchIndexController2_0.java:77` | ✅ fixed |
| 11 | `SearchIndexController2_0.java:81` | ✅ fixed |
| 12 | `SearchIndexController2_0.java:81` | ✅ fixed |
| 13 | `ConceptResource1_8.java:567` | ✅ fixed |
| 14 | `RestUtil.java:521` | ✅ fixed |
| 15 | `AuthorizationFilter.java:108` | ✅ fixed |

Alle 9 oorspronkelijke `java/log-injection`-findings zijn gesloten, inclusief het
9e alert in `AuthorizationFilter.java:108` (de regel die in commit `8521cdc` voor
issue #65 al was herschreven naar parameterized logging — die herschrijving plus de
`sanitizeForLog`-call op `attemptedUser` bleek samen voldoende voor CodeQL om de
taint-flow als gemitigeerd te beschouwen).

> **Update — gesanitized i.p.v. gedismissed:** dezelfde herschrijving voor issue #65
> introduceerde 4 *nieuwe* `java/log-injection`-alerts in `AuthorizationFilter.java`
> (regels 72, 84, 115, 122 — alert-nrs `808`–`811`), die eerst als **"false positive"**
> waren gedismissed *zonder* onderbouwing. Omdat die classificatie afhing van de
> (niet in deze module aanwezige) log-appender-configuratie en dus niet hard
> aantoonbaar was, zijn de user-controlled waarden (request-URI, Basic-Auth-
> gebruikersnaam én de exception-message) nu **daadwerkelijk via
> `RestUtil.sanitizeForLog()`** geleid — dezelfde taint-barrier als de 9 eerder
> gefixte alerts. De 4 alerts sluiten daarmee op de eerstvolgende CodeQL-herscan op
> `main`; die run nog toevoegen als bewijsartefact (zelfde patroon als §1 hierboven).
> Zie ook [F1-restpunt in 04-code-review](../04-code-review/04.md#21-bevinding-1--log-injection-cwe-117-via-rest-request-parameters).

✅ Hiermee is de detect → triage → mitigeer → verifieer-cyclus voor F1 gesloten en
kwantitatief aangetoond (9 → 0 open alerts).

---

## 2. SQ8 / CWE-79 — Reflected XSS in Swagger debug-endpoint

**Bevinding:** [04-code-review §2.2](../04-code-review/04.md#22-bevinding-2--reflected-xss-in-swagger-debug-endpoint-cwe-79-)
(CodeQL `java/xss`, alert **#1**, severity high). `SwaggerDocController.debug()`
reflecteert de query-parameter `tag` ongesanitized in een HTML-respons
(`return "<h1>Debugging Tag: " + tag + "</h1>";`).

**Besluit: geen code-fix — bewust niet gemitigeerd.**

De bevinding is vastgesteld via statische analyse en is inhoudelijk correct.
In de pentest-omgeving retourneert het endpoint echter HTTP 404 omdat Swagger
is uitgeschakeld via de global property `webservices.rest.enableSwaggerDocs`
(hardening-checklist §1). Daardoor is **functionele validatie van een code-fix
niet mogelijk**: elke pentest-hertest bevestigt slechts de 404, niet de werking
van output-encoding.

**Toegepaste maatregel (configuratie-niveau):**
- De gehele `/apiDocs`-boom is uitschakelbaar via `webservices.rest.enableSwaggerDocs`
  (zie [hardening-checklist §1](../01-security-audit/threat-model/hardening-checklist.md#1-onnodige-features-uitschakelen)).
  In productie op `false` retourneert het endpoint HTTP 404 — de aanvalsvector
  bestaat dan niet.
- Dit is de gekozen mitigatie: **least-functionality** (het endpoint is niet nodig
  in productie) i.p.v. output-encoding.

**Resterend risico:** als `webservices.rest.enableSwaggerDocs` ooit op `true` wordt
gezet zonder aanvullende maatregel, is de XSS bereikbaar. De aanbevolen definitieve
fix (`HtmlUtils.htmlEscape` op de `tag`-parameter) blijft als technische schuld
genoteerd voor dat scenario.

**Status:** ❌ **Open — bewust niet gemitigeerd** (code-fix niet valideerbaar via pentest;
productie-blootstelling afgedekt door configuratie-gating).

---

## 3. `AuthorizationFilter` — session-timeout fall-through (CWE-302, opgelost) + alert #908 (geaccepteerd risico)

### 3a. Session-timeout fall-through (CWE-302) — opgelost

**Bevinding:** [04-code-review §2.3](../04-code-review/04.md#23-bevinding-3--user-controlled-authentication-bypass-in-authorizationfilter-cwe-302-)
(handmatige review). Na een verlopen sessie werd
`sendError(401)` aangeroepen **zonder `return`**, waardoor de uitvoering doorliep naar
`chain.doFilter()` — de 401 werd verstuurd maar de request werd alsnog doorgegeven.
Dit is een fall-through (CWE-302-patroon), **niet** het CodeQL-alert #908 (zie §3b).

**Mitigatie:** `return;` toegevoegd direct na de `SESSION_TIMEOUT`-`sendError`
(`AuthorizationFilter.java`), zodat een timed-out request de keten daadwerkelijk stopt.

- **Realisatie/tooling:** Claude Code (Opus 4.8); één-regel-fix conform de aanbeveling
  in 04-code-review, handmatig gereviewd op neveneffecten (de "best-effort"-flow voor
  *credentials* blijft ongewijzigd — alleen de expliciete timeout-tak stopt nu).

**Validatie (voor/na):**

| | Vóór | Na |
|---|---|---|
| Verlopen sessie | 401 gezet, request loopt door naar `chain.doFilter()` | 401 gezet, keten gestopt (`return`) |
| `AuthorizationFilterLoggingTest` (12 tests) | groen | **groen** (geen regressie) |
| `chain.doFilter()` na een 401-op-timeout | bereikbaar (fall-through) | onbereikbaar (`return`) |

### 3b. CodeQL `java/user-controlled-bypass` #908 — geaccepteerd risico (false positive)

**Bevinding (geverifieerd 2026-06-15):** alert **#908** wijst naar
[`AuthorizationFilter.java:119`](../../omod-common/src/main/java/org/openmrs/module/webservices/rest/web/filter/AuthorizationFilter.java#L119)
— de aanroep `Context.authenticate(userAndPass[0], userAndPass[1])`. CodeQL-meldtekst:
*"sensitive method may not be executed depending on a condition which flows from
user-controlled value."* De aanroep is control-dependent op `if (basicAuth != null)` →
`if (basicAuth.startsWith("Basic"))`, beide afgeleid van de user-controlled
`Authorization`-header.

> ⚠️ **Correctie t.o.v. eerdere versies van dit document:** #908 is **niet** de
> session-timeout fall-through uit §3a, maar deze optionele-Basic-Auth-tak. De
> `return`-fix uit §3a sluit dit alert daarom niet — #908 stond na meerdere
> CodeQL-herscans op `main` (met `e3f3c7d` aanwezig) nog steeds `open`.

**Analyse — niet exploiteerbaar (faalt gesloten):** het overslaan van `Context.authenticate`
verleent géén toegang; de request blijft *unauthenticated* en OpenMRS handhaaft autorisatie
downstream (gooit een exception zodra een niet-geauthenticeerde caller een beschermde
resource benadert). De filter is *bewust* best-effort — Javadoc: *"It will not fail on
invalid or missing credentials. We count on the API to throw exceptions…"*. De conditionele
tak **verlaagt** dus het privilege i.p.v. het te verlenen. Het blok is bovendien **upstream
OpenMRS-code** (eerste import `eb72694`), niet door dit team geïntroduceerd.

**Waarom geen code-fix:** de control-dependency (alleen authenticeren *als* er een
Basic-header is) is inherent aan optionele Basic-Auth; een guard-clause of helper-extractie
verwijdert die dependency niet. De enige wijziging die het alert wél zou sluiten —
authenticatie *verplicht* maken — breekt het gedocumenteerde contract en de hele
REST-module (sessie/cookie-auth, publieke endpoints) en is dus een regressie, geen fix.
Dit is geen injectie-query, dus er is geen sanitizer/barrier (zoals bij CWE-117) toe te voegen.

**Besluit:** geaccepteerd risico, conform de afgesproken omgang met false positives
(Sprint 2, CI-scanning). #908 wordt in CodeQL gedismissed met onderbouwing
(reason: *won't fix* / accepted risk) en verwijzing naar deze sectie. Uit te voeren door
een repo-beheerder:

```bash
gh api -X PATCH repos/GrannyGuard/webservices-rest-audit/code-scanning/alerts/908 \
  -f state=dismissed -f dismissed_reason="won't fix" \
  -f dismissed_comment="Accepted risk (not exploitable) — best-effort optional Basic-Auth, fails closed, enforced downstream. See docs/security/06-mitigatie-en-validatie/06.md section 3b."
```

**Bewijsartefact:** na dismissal toont `gh api .../code-scanning/alerts/908`
`"state": "dismissed"` + `"dismissed_reason": "won't fix"` met de onderbouwing als comment.

---

## 4. SQ9 — Host-header injectie in de Swagger-specificatie

**Bevinding:** [01-security-audit §3.7 (TM-S4 / SQ9)](../01-security-audit/01.md#37-threat-model--c4-architectuur--stride-analyse).
`SwaggerSpecificationController` nam de client-`Host`- en `X-Forwarded-Proto`-headers
ongevalideerd over in de gepubliceerde OpenAPI-spec.

**Mitigatie:**
- `RestUtil.resolveSwaggerHost(host)` valideert de `Host` tegen de allow-list
  `webservices.rest.swaggerAllowedHosts`; een niet-toegestane host valt terug op de
  canonieke (eerste) host i.p.v. de client-waarde.
- `RestUtil.sanitizeScheme(scheme, default)` staat alleen `http`/`https` toe (blokkeert
  `X-Forwarded-Proto`-injectie).
- Bedraad in `SwaggerSpecificationController`; global properties in `config.xml`. Zie
  [hardening-checklist §10](../01-security-audit/threat-model/hardening-checklist.md#10-openapi-specificatie-alleen-naar-specifieke-allowed-hosts).
- **Realisatie/tooling:** Claude Code (Opus 4.8); logica bewust in testbare helpers in
  `RestUtil` gezet i.p.v. inline in de controller.

**Validatie (voor/na):** unit tests `SwaggerHardeningRestUtilTest` — **8/8 groen**:

| Test | Bewijst |
|---|---|
| `resolveSwaggerHost_shouldReturnRequestHostWhenAllowListEmpty` | legacy-gedrag bij lege lijst |
| `resolveSwaggerHost_shouldReturnRequestHostWhenItIsOnTheAllowList` | toegestane host blijft |
| `resolveSwaggerHost_shouldFallBackToFirstAllowedHostWhenRequestHostNotAllowed` | gespoofte host → canonieke host |
| `sanitizeScheme_shouldKeepHttpAndHttps` / `…FallBackToDefaultForUnsafeScheme` | alleen http/https gereflecteerd |

`mvn clean install` groen.

---

## 5. Attack-surface-reductie (preventieve maatregelen)

Naast de bovenstaande code-fixes zijn **preventieve** maatregelen genomen die de
*kans* en *impact* van misbruik verlagen i.p.v. één CodeQL-alert te sluiten. Ze zijn
per stuk gedocumenteerd met status, NEN-7510-control en koppeling in de
[hardening-checklist](../01-security-audit/threat-model/hardening-checklist.md):

| Maatregel | NEN-7510 | Effect |
|---|---|---|
| Swagger/OpenAPI uitschakelbaar (`enableSwaggerDocs`) — §1 | A.8.3 | minder blootstelling SQ8/SQ9 in productie |
| Least-privilege runtime DB-user (kan nooit `DROP`) — §2 | A.8.2/A.8.3 | beperkt impact van SQL-injectie/escalatie (TM-E2) |
| Signed artifacts (cosign + SLSA-provenance + SBOM) — §8 | A.5.23/A.8.32 | verifieerbare supply chain (CD1) |
| Alle CI-actions SHA-pinned — §9 | A.5.23/A.8.32 | onveranderbare build-toolchain |

---

## 6. F-01 — Geen rate limiting (CWE-307)

**Bevinding:** [PentestReport §6 — F-01](../05-penetration-tests/PentestReport-OpenMRS.md#6-f-01--geen-rate-limiting-cwe-307) — NEN-7510 A.8.5 (toegangsbeveiliging), A.8.15 (logging beveiligingsgebeurtenissen).

**Mitigatie:**
- **Aanpak:** in-memory IP-rate-limiter in `AuthorizationFilter`: bounded `ConcurrentHashMap<String, long[]>` (max 10.000 entries als safety valve) bijhoudt `{ failedAttemptCount, windowStartEpochMs }` per bron-IP. Max 10 mislukte pogingen per 60-secondenvenster → HTTP 429 + `return` vóór `Context.authenticate()` aangeroepen wordt; teller reset bij succesvolle authenticatie via `clearFailedAttempts`. Log op WARN: `ACCESS_BLOCKED rate_limited=true ip=[...] uri=[...]` (NEN-7510 A.8.15).
- **Gewijzigd bestand:** [`omod-common/.../web/filter/AuthorizationFilter.java`](../../omod-common/src/main/java/org/openmrs/module/webservices/rest/web/filter/AuthorizationFilter.java) — nieuwe methoden `isRateLimited`, `recordFailedAttempt`, `clearFailedAttempts`, `resetRateLimitState` + uitbreiding `doFilter` (commit `59bf421`).
- **Realisatie/tooling:** Claude Code (Sonnet 4.6); `ConcurrentHashMap` i.p.v. Guava Cache gekozen voor Java 8-compatibiliteit zonder extra dependency; benign read-race buiten `compute` bewust geaccepteerd (voldoende precisie voor rate limiting).

**Validatie (voor/na):**

| | Vóór fix | Ná fix |
|---|---|---|
| 11e achtereenvolgende mislukte Basic Auth-poging (Kali — 2026-06-16) | HTTP 200 (nooit geblokkeerd) | **HTTP 429** |
| Docker-log op 11e poging | geen blokkade-entry | `WARN ACCESS_BLOCKED rate_limited=true ip=[172.22.0.1]` |
| `AuthorizationFilterRateLimitTest` (6 nieuwe tests) | niet aanwezig | **6/6 groen** |
| `AuthorizationFilterLoggingTest` (12 bestaande tests) | 12 groen | **12 groen** (geen regressie) |

`mvn clean install` groen — 127 tests, 0 fouten.

---

## 7. F-02 — Gebruikersenumeratie (CWE-204)

**Bevinding:** [PentestReport §7 — F-02](../05-penetration-tests/PentestReport-OpenMRS.md#7-f-02--gebruikersenumeratie-cwe-204) — NEN-7510 A.8.5 (toegangsbeveiliging), A.8.15 (logging beveiligingsgebeurtenissen).

**Mitigatie:**
- **Aanpak (Methode A):** `Context.requirePrivilege(PrivilegeConstants.GET_USERS)` toegevoegd als eerste instructie van `doGetAll()` en `doSearch()` in `UserResource1_8`. Elke aanroep op `/user` (lijst én zoeken) vereist nu het OpenMRS-privilege `Get Users`; gebruikers zonder dit privilege ontvangen een `ContextAuthenticationException`.
- **Aanpak (Methode B):** `reason=[{}]`-veld verwijderd uit de `AUTH_FAILURE`-logentry in `AuthorizationFilter.doFilter()`. Alle auth-fouten loggen nu uniform — geen onderscheid meer tussen fout wachtwoord en OpenMRS-account-lockout (~7 pogingen per account).
- **Gewijzigd bestand (A):** [`omod/.../resource/openmrs1_8/UserResource1_8.java`](../../omod/src/main/java/org/openmrs/module/webservices/rest/web/v1_0/resource/openmrs1_8/UserResource1_8.java) — `doGetAll()` r. 422, `doSearch()` r. 248.
- **Gewijzigd bestand (B):** [`omod-common/.../web/filter/AuthorizationFilter.java`](../../omod-common/src/main/java/org/openmrs/module/webservices/rest/web/filter/AuthorizationFilter.java) — r. 182 (`reason=` verwijderd uit `log.warn`).
- **Realisatie/tooling:** Claude Code (Sonnet 4.6); privilege-check-patroon ontleend aan bestaand `LoggedInUsersController2_0.java`; `reason=`-verwijdering na analyse van lockout-side-channel in applicatielogs; 3 nieuwe unit tests.

**Validatie (voor/na):**

| | Vóór fix | Ná fix |
|---|---|---|
| `GET /user` als laag-geprivilegieerde `testuser` | HTTP 200 + volledige gebruikerslijst | HTTP 500 — `"Privileges required: Get Users"` |
| `GET /user?q=admin` als `testuser` | HTTP 200 + zoekresultaat | HTTP 500 — `"Privileges required: Get Users"` |
| `AUTH_FAILURE`-log na OpenMRS-lockout (poging 8+) | `reason=[Invalid number of connection attempts...]` | geen `reason=`-veld — identiek aan eerdere pogingen |
| `UserResource2_0Test` (2 nieuwe privilege-tests) | niet aanwezig | **2/2 groen** |
| `AuthorizationFilterRateLimitTest` uniforme-log-test (1 nieuwe test) | niet aanwezig | **1/1 groen** |

`mvn clean install` groen — 1832 tests, 0 fouten.

---

## 8. Verantwoording (AI-)tooling

De realisatie is uitgevoerd met **Claude Code (Opus 4.8)** als pair-programmer, binnen
een vaste werkwijze:

1. **Lokaliseren** — bevindingen kwamen uit CodeQL (`gh api .../code-scanning/alerts`),
   het threat model (STRIDE) en handmatige review; niet uit een blinde AI-suggestie.
2. **Genereren** — de tool stelde de fix + tests voor (centrale helpers in `RestUtil`,
   `HtmlUtils.htmlEscape`, de `return`-fix).
3. **Reviewen** — elke wijziging is handmatig beoordeeld op correctheid en
   neveneffecten vóór commit (bv. bewust géén eigen escaping maar de Spring-bibliotheek;
   bewust de credentials-flow van de filter ongemoeid laten).
4. **Valideren** — `mvn clean install` + gerichte unit tests ná elke wijziging; geen
   commit zonder groene build.

GitHub Copilot Autofix (GitHub Advanced Security) is daarnaast beschikbaar op
CodeQL-alerts (zie [04-code-review §1](../04-code-review/04.md)); suggesties zijn niet
blind overgenomen.

---

## 9. Kritische reflectie

- **Sterk:** de code-fixes zijn klein, bibliotheek-gebaseerd en testbaar gemaakt
  (helpers in `RestUtil` i.p.v. inline, rate limiter als afzonderlijke methoden in
  `AuthorizationFilter`), wat reproduceerbare unit-validatie mogelijk maakt en de
  kans op regressie minimaliseert (bevestigd: 127 tests, 0 fouten na elke fix).
- **Validatiediepte — inmiddels afgesloten voor de kritische bevindingen:** unit tests
  bewijzen de *logica*, niet de volledige HTTP-keten. Voor SQ8 is de **CodeQL-herscan**
  de definitieve "na"-meting (§2); #908 is een **onderbouwde dismissal** (§3b). Voor
  F-07 (log injection) en F-01 (rate limiting) is de cirkel inmiddels kwantitatief
  gesloten via **pentest-hertest op Kali** (2026-06-16): F-07 toont sanitisatie in
  `AuthorizationFilter` met restrisico in OpenMRS Core (out-of-scope, gedocumenteerd);
  F-01 bevestigt HTTP 429 op de 11e poging; F-02 bevestigt privilege-blokkade en uniforme logs. Zie restpunten voor nog openstaande hertests (F-03 e.v.).
- **Beperking — gating defaultt op `true`:** `enableSwaggerDocs` staat standaard aan om
  bestaand gedrag niet te breken; de productie-winst hangt dus af van een correcte
  OTAP-configuratie (`false`). Bewuste afweging (compatibiliteit vs. secure-by-default),
  als zodanig gedocumenteerd.
- **Beperking — least-privilege DB:** het mechanisme is aanwezig, maar de runtime-switch
  naar het DML-only account is een deploy-stap die in deze omgeving niet
  container-geverifieerd kon worden (Docker-daemon niet beschikbaar).
- **Tooling:** AI (Claude Code Opus 4.8 / Sonnet 4.6) versnelde lokaliseren + boilerplate
  (tests, helpers), maar de beveiligingsbeslissingen (welke fix, welke afweging) zijn
  menselijk genomen en geverifieerd; CodeQL/unit tests/pentest — niet de AI — zijn het bewijs.

---

## Restpunten

- [x] **Mitigaties gedocumenteerd** — §1–§6 hierboven.
- [x] **(AI-)tooling verantwoord** — §7.
- [x] **Voor/na per bevinding** — §1–§6.
- [x] **Kritische reflectie** — §8.
- [x] **CodeQL-herscan als artefact** voor SQ8 (`java/xss` #1) toegevoegd — §2
  (run `27471405779`, alert → `fixed`).
- [ ] **#908 (`java/user-controlled-bypass`) dismissen** met de onderbouwing uit §3b
  (reason: *won't fix* / accepted risk) — geverifieerd niet-exploiteerbaar; resteert
  alleen de repo-beheerder-actie. **Handmatig uitvoeren door repo-beheerder:**
  ```bash
  gh api -X PATCH repos/GrannyGuard/webservices-rest-audit/code-scanning/alerts/908 \
    -f state=dismissed \
    -f dismissed_reason="won't fix" \
    -f dismissed_comment="Accepted risk — best-effort optional Basic-Auth, fails closed, enforced downstream. See docs/security/06-mitigatie-en-validatie/06.md §3b."
  ```
- [x] **Pentest-hertest F-07** uitgevoerd 2026-06-16 met echte `$'admin\n...'`-syntax:
  - `AuthorizationFilter` (REST module): ✅ newlines gestript, injectie op één AUTH_FAILURE-regel gehouden
  - `HibernateContextDAO.authenticate()` (OpenMRS Core): ❌ logt ongesaniteerd — neppe `Successful login`-regels verschijnen als zelfstandige INFO-entries. **Restrisico buiten module-scope** (upstream OpenMRS Core fix vereist). Zie `pentestfindings.md §4.3b` voor ruwe output.
- [x] **F-01 (CWE-307) — rate limiting:** HTTP 429 geïmplementeerd in `AuthorizationFilter`; Kali-hervalidatie 2026-06-16 bevestigt blokkade op 11e poging. Zie §6.
- [x] **F-02 (CWE-204) — gebruikersenumeratie:** privilege check op `/user` (Methode A) + uniforme AUTH_FAILURE log (Methode B); Kali-hervalidatie 2026-06-16 bevestigd. Zie §7.
- [ ] **Hoogste resterende prioriteit (nog niet gemitigeerd):** **F-03** (session fixation — sessie-ID-rotatie na login). Vereist pentest-hervalidatie.
- [ ] **CWE-117 log-injection (4 extra alerts 808–811):** code-fix toegepast
  (`sanitizeForLog` op `AuthorizationFilter.java:72,84,115,122`); CodeQL-herscan
  op `main` nog toevoegen als sluitend bewijsartefact.
